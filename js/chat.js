/* ==========================================================================
   ISLOH — Chat module (reserved)
   Empty on purpose. pages/student/chat.html is currently static markup.
   Send/receive, thread switching, and typing-indicator logic belongs here
   so pages/instructor/messages.html (Sprint 9) can reuse the same module
   instead of re-implementing the chat shell's behavior.
   ========================================================================== */
